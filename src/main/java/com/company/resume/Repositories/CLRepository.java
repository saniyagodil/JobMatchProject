package com.company.resume.Repositories;

import com.company.resume.Models.CoverLetter;
import org.springframework.data.repository.CrudRepository;

public interface CLRepository extends CrudRepository<CoverLetter, Long> {
}
